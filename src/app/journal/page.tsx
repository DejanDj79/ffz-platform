import { ModulePlaceholder } from "@/components/shell/ModulePlaceholder";

export default function JournalPage() {
  return (
    <ModulePlaceholder
      title="Trade Journal"
      description="This route is reserved so the navigation and platform structure are stable before we start the Journal module."
      nextStep="Backend + persistent trading data, then Journal development."
    />
  );
}
